[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.coverage.run]
source = ["lorentz_transformer"]

[tool.coverage.report]
exclude_lines = [
    'if __name__ == "__main__"',
    "pragma: no cover",
]
